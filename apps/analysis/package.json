{
    "name": "@app/analysis",
    "version": "0.1.0",
    "scripts": {
        "clean": "git clean -xdf .cache dist .turbo __pycache__ '*.py[cod]' .pytest_cache .mypy_cache build",
        "env:copy": "cp -n .env.dist .env || true",
        "start": "bash -c 'source .venv/bin/activate && poetry run python -m analysis'",
        "dev": "bash -c 'source .venv/bin/activate && poetry run python -m analysis'",
        "test": "bash -c 'source .venv/bin/activate && poetry run pytest'",
        "lint": "bash -c 'source .venv/bin/activate && poetry run ruff check .'"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "type": "commonjs"
}
